# domain: healthcare
# project_dir: my-hospital
# seed: 42

# start_date: 2026-01-01
# end_date: 2026-01-03

# # --- Testing Mode ---
# # cron_interval_minutes: 1

# # --- Production Mode ---
# # Uncomment below and comment above for real-world deployment
# cron_daily_time: "14:50"

# Multi-domain mode (optional): if present, takes priority over top-level settings.
# Each domain runs independently with its own project_dir, checkpoint, and backup.
domains:
  - domain: retail
    project_dir: my-new-shop
    seed: 42
    start_date: 2026-01-01
    end_date: 2026-01-03
    cron_interval_minutes: 1
    # cron_daily_time: "12:00"

  - domain: healthcare
    project_dir: my-new-hospital
    seed: 42
    start_date: 2026-01-01
    end_date: 2026-01-03
    cron_interval_minutes: 1
    # cron_daily_time: "12:00"