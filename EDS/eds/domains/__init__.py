"""Business domains built on the platform.

Each subpackage is one self-contained simulation domain owning its own entity
schemas, generators, validation rules and configuration. Retail is the first;
Healthcare, Banking and Manufacturing can be added beside it without any
change to :mod:`eds.core`, :mod:`eds.platform` or :mod:`eds.adapters`
(PADR-002).
"""
